#include <iostream>
#include "Linear_Array.h"

void insertion_sort(int* DATA,int UB,int LB = 0)
{
    int min = DATA[0] ;
    int ptr = 2 ;
    for(int i = LB + 1 ; i < UB ; i++ )
    {
        while (DATA[i] > DATA[ptr]) 
        {
            if (DATA[ptr] < DATA[i])
            {
                
            }
        }
    }
}
